<Window x:Class="AmpzDesktopBooster.DockerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:AmpzDesktopBooster.Services.Localization"
        Title="Docker"
        Width="1100" Height="660"
        WindowStyle="None" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False" Topmost="True"
        MinWidth="700" MinHeight="420"
        Background="#1E1E1E" UseLayoutRounding="True"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Filas: verde si el contenedor está corriendo (IsRunning), gris si no -->
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#9A9A9A" />
            <Setter Property="Padding" Value="4,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter Content="{TemplateBinding Content}"
                                                  Columns="{TemplateBinding GridView.ColumnCollection}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Foreground" Value="#55EE99" />
                                <Setter TargetName="Bd" Property="Background" Value="#10221A" />
                            </DataTrigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2D4A57" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#262626" />
            <Setter Property="Foreground" Value="#9A9A9A" />
            <Setter Property="BorderBrush" Value="#333" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Background" Value="#2A2A2A" />
            <Setter Property="Foreground" Value="#F2F2F2" />
            <Setter Property="BorderBrush" Value="#444" />
        </Style>
    </Window.Resources>

    <Border BorderBrush="#33FFFFFF" BorderThickness="1" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Status + acciones -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <TextBlock x:Name="StatusText" Foreground="#9A9A9A" FontSize="13" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="RefreshBtn"  Content="{loc:Translate Docker.Refresh}" />
                    <Button x:Name="StartBtn"    Content="{loc:Translate Docker.Start}" />
                    <Button x:Name="StopBtn"     Content="{loc:Translate Docker.Stop}" />
                    <Button x:Name="CopyPortBtn" Content="{loc:Translate Docker.CopyPort}" />
                </StackPanel>
            </Grid>

            <TextBox Grid.Row="1" x:Name="FilterBox" Background="#2A2A2A" Foreground="#F2F2F2"
                     BorderBrush="#444" Padding="6,4" FontSize="13" Margin="0,0,0,8" />

            <Border Grid.Row="2" Background="#161616" CornerRadius="4" Padding="2">
                <ListView x:Name="ContainersList" Background="Transparent" BorderThickness="0" FontSize="13"
                          SelectionMode="Extended">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="{loc:Translate Docker.ColName}"     Width="240" DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="{loc:Translate Docker.ColImage}"    Width="280" DisplayMemberBinding="{Binding Image}" />
                            <GridViewColumn Header="{loc:Translate Docker.ColStatus}"   Width="200" DisplayMemberBinding="{Binding Status}" />
                            <GridViewColumn Header="{loc:Translate Docker.ColInternal}" Width="120" DisplayMemberBinding="{Binding InternalPorts}" />
                            <GridViewColumn Header="{loc:Translate Docker.ColExposed}"  Width="110" DisplayMemberBinding="{Binding ExposedPorts}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>

            <TextBlock Grid.Row="3" Foreground="#6E6E78" FontSize="10" Margin="0,8,0,0"
                       Text="{loc:Translate Docker.HintBar}" />
        </Grid>
    </Border>
</Window>
